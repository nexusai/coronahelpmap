rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{users/{userId} {
      allow read: true;
      allow write: if request.auth.uid == userId 
        && request.auth.uid == reuquest.resource.data.uid;
    }
  }
}